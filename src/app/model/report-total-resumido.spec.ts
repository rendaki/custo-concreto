import { ReportTotalResumido } from './report-total-resumido';

describe('ReportTotalResumido', () => {
  it('should create an instance', () => {
    expect(new ReportTotalResumido()).toBeTruthy();
  });
});
